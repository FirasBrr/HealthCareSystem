{% extends 'base.html.twig' %}

{% block title %}MedicalApp - Login{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        :root {
            --primary: #2c7be5;
            --primary-dark: #1a68d1;
            --secondary: #6e84a3;
            --success: #00d97e;
            --light: #f9fbfd;
            --dark: #12263f;
            --gradient: linear-gradient(135deg, #2c7be5 0%, #6e84a3 100%);
        }

        .login-section {
            background: var(--gradient);
            color: white;
            padding: 60px 0;
            position: relative;
            overflow: hidden;
            min-height: 100vh;
            display: flex;
            align-items: center;
        }

        .login-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 1000" opacity="0.1"><path fill="white" d="M500,250c138,0,250,112,250,250S638,750,500,750S250,638,250,500S362,250,500,250z"/></svg>') repeat;
            background-size: 150px;
        }

        .login-card {
            background: white;
            border-radius: 20px;
            padding: 30px 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.1);
            position: relative;
            z-index: 2;
        }

        .login-title {
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .login-subtitle {
            color: var(--secondary);
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 0.95rem;
        }

        .form-group-custom {
            margin-bottom: 1.2rem;
        }

        .form-label-custom {
            color: var(--dark);
            font-weight: 600;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }

        .form-control-custom {
            border: 2px solid #e3ebf6;
            border-radius: 12px;
            padding: 10px 14px;
            font-size: 0.95rem;
            transition: all 0.3s ease;
            background: #f9fbfd;
            width: 100%;
        }

        .form-control-custom:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(44, 123, 229, 0.1);
            background: white;
            outline: none;
        }

        .btn-login {
            background: var(--primary);
            color: white !important;
            border: none;
            padding: 12px 24px;
            font-size: 1rem;
            font-weight: 600;
            border-radius: 12px;
            width: 100%;
            transition: all 0.3s ease;
            margin-top: 1rem;
        }

        .btn-login:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(44, 123, 229, 0.3);
            color: white !important;
        }

        .register-link {
            text-align: center;
            margin-top: 1.5rem;
            color: var(--secondary);
            font-size: 0.9rem;
        }

        .register-link a {
            color: var(--primary);
            text-decoration: none;
            font-weight: 600;
        }

        .register-link a:hover {
            text-decoration: underline;
        }

        .feature-icon-small {
            width: 50px;
            height: 50px;
            background: var(--gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            margin-bottom: 1rem;
        }

        .benefits-list {
            list-style: none;
            padding: 0;
        }

        .benefits-list li {
            padding: 0.4rem 0;
            color: var(--secondary);
            font-size: 0.85rem;
        }

        .benefits-list li i {
            color: var(--success);
            margin-right: 0.5rem;
            width: 20px;
        }

        .alert-custom {
            border-radius: 12px;
            border: none;
            padding: 12px 15px;
            margin-bottom: 1.5rem;
            font-size: 0.9rem;
        }

        .forgot-password {
            text-align: center;
            margin-top: 1rem;
        }

        .forgot-password a {
            color: var(--primary);
            text-decoration: none;
            font-size: 0.85rem;
        }

        .forgot-password a:hover {
            text-decoration: underline;
        }

        /* Demo notice styles */
        .demo-notice {
            background: linear-gradient(135deg, #f39c12 0%, #e67e22 100%);
            border-radius: 12px;
            padding: 12px;
            margin-bottom: 20px;
            color: white;
            border: none;
            position: relative;
            overflow: hidden;
        }

        .demo-notice::before {
            content: '🚀';
            position: absolute;
            right: -10px;
            top: -10px;
            font-size: 50px;
            opacity: 0.1;
        }

        .demo-notice h6 {
            margin-bottom: 8px;
            font-weight: 700;
            font-size: 0.9rem;
        }

        .demo-notice p {
            margin-bottom: 4px;
            font-size: 0.8rem;
        }

        .demo-notice code {
            background: rgba(255,255,255,0.2);
            padding: 2px 5px;
            border-radius: 6px;
            font-size: 0.75rem;
            color: white;
        }

        .demo-notice hr {
            margin: 8px 0;
        }

        /* ============================================ */
        /* RESPONSIVE STYLES */
        /* ============================================ */

        /* Tablet and up */
        @media (min-width: 768px) {
            .login-section {
                padding: 80px 0;
            }

            .login-card {
                padding: 50px 40px;
            }

            .login-title {
                font-size: 2.2rem;
            }

            .login-subtitle {
                font-size: 1rem;
            }

            .demo-notice {
                padding: 15px;
            }

            .demo-notice h6 {
                font-size: 1rem;
            }

            .demo-notice p {
                font-size: 0.85rem;
            }
        }

        /* Desktop */
        @media (min-width: 992px) {
            .login-title {
                font-size: 2.5rem;
            }

            .login-subtitle {
                font-size: 1.1rem;
            }

            .demo-notice h6 {
                font-size: 1rem;
            }
        }

        /* Small mobile */
        @media (max-width: 480px) {
            .login-section {
                padding: 40px 0;
            }

            .login-card {
                padding: 20px 15px;
            }

            .login-title {
                font-size: 1.5rem;
            }

            .form-label-custom {
                font-size: 0.85rem;
            }

            .form-control-custom {
                padding: 8px 12px;
                font-size: 0.9rem;
            }

            .btn-login {
                padding: 10px 20px;
                font-size: 0.9rem;
            }

            .demo-notice {
                padding: 10px;
            }

            .demo-notice h6 {
                font-size: 0.85rem;
            }

            .demo-notice p {
                font-size: 0.75rem;
            }

            .demo-notice code {
                font-size: 0.7rem;
            }
        }
    </style>
{% endblock %}

{% block body %}
    <!-- Login Section -->
    <section class="login-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-lg-10">
                    <div class="login-card">
                        <div class="row">
                            <!-- Login Form -->
                            <div class="col-12 col-lg-6">
                                <h1 class="login-title">Welcome Back</h1>
                                <p class="login-subtitle">Sign in to your MedicalApp account</p>

                                <!-- DEMO NOTICE -->
                                <div class="demo-notice">
                                    <h6><i class="fas fa-info-circle me-2"></i>Live Demo Access</h6>
                                    <p class="mb-1"><strong>Admin Account:</strong></p>
                                    <p class="mb-1"><i class="fas fa-envelope me-2"></i>Email: <code>admin@mail.com</code></p>
                                    <p><i class="fas fa-lock me-2"></i>Password: <code>admin123</code></p>
                                    <hr class="my-2" style="border-color: rgba(255,255,255,0.3);">
                                    <p class="mb-0 small"><i class="fas fa-star me-1"></i> Use this account to explore all admin features</p>
                                </div>

                                {% if error %}
                                    <div class="alert alert-danger alert-custom">
                                        <i class="fas fa-exclamation-circle me-2"></i>{{ error.messageKey|trans(error.messageData, 'security') }}
                                    </div>
                                {% endif %}

                                {% for flash_success in app.flashes('success') %}
                                    <div class="alert alert-success alert-custom">
                                        <i class="fas fa-check-circle me-2"></i>{{ flash_success }}
                                    </div>
                                {% endfor %}

                                <form method="post">
                                    <div class="form-group-custom">
                                        <label for="email" class="form-label-custom">Email Address</label>
                                        <input type="email" value="{{ last_username }}" name="email" id="email" class="form-control form-control-custom" placeholder="Enter your email" autocomplete="email" required autofocus>
                                    </div>

                                    <div class="form-group-custom">
                                        <label for="password" class="form-label-custom">Password</label>
                                        <input type="password" name="password" id="password" class="form-control form-control-custom" placeholder="Enter your password" autocomplete="current-password" required>
                                    </div>

                                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                                    <div class="form-group-custom">
                                        <div class="form-check">
                                            <input class="form-check-input" type="checkbox" name="_remember_me" id="remember_me">
                                            <label class="form-check-label" for="remember_me" style="color: var(--secondary);">
                                                Remember me
                                            </label>
                                        </div>
                                    </div>

                                    <button type="submit" class="btn btn-login">
                                        <i class="fas fa-sign-in-alt me-2"></i>Sign In
                                    </button>

                                    <div class="forgot-password">
                                        <a href="#">Forgot your password?</a>
                                    </div>
                                </form>

                                <div class="register-link">
                                    Don't have an account? <a href="{{ path('app_register') }}">Create one here</a>
                                </div>
                            </div>

                            <!-- Benefits Sidebar - Hidden on mobile, visible on tablet+ -->
                            <div class="d-none d-lg-block col-lg-5 offset-lg-1 mt-5 mt-lg-0">
                                <div class="feature-icon-small">
                                    <i class="fas fa-stethoscope"></i>
                                </div>
                                <h4 style="color: var(--dark); margin-bottom: 1rem;">Welcome to MedicalApp</h4>

                                <ul class="benefits-list">
                                    <li><i class="fas fa-check-circle"></i> Manage your appointments</li>
                                    <li><i class="fas fa-check-circle"></i> Access your health records</li>
                                    <li><i class="fas fa-check-circle"></i> Connect with healthcare providers</li>
                                    <li><i class="fas fa-check-circle"></i> Secure messaging</li>
                                    <li><i class="fas fa-check-circle"></i> Prescription management</li>
                                    <li><i class="fas fa-check-circle"></i> 24/7 access to your health data</li>
                                </ul>

                                <div class="mt-4 p-3" style="background: var(--light); border-radius: 12px;">
                                    <div class="d-flex align-items-center">
                                        <i class="fas fa-shield-alt text-success me-3" style="font-size: 1.3rem;"></i>
                                        <div>
                                            <h6 class="mb-1" style="color: var(--dark);">Your Data is Secure</h6>
                                            <p class="mb-0 small" style="color: var(--secondary);">Industry-standard encryption</p>
                                        </div>
                                    </div>
                                </div>

                                <div class="mt-3 p-3" style="background: var(--light); border-radius: 12px;">
                                    <div class="d-flex align-items-center">
                                        <i class="fas fa-mobile-alt text-primary me-3" style="font-size: 1.3rem;"></i>
                                        <div>
                                            <h6 class="mb-1" style="color: var(--dark);">Access Anywhere</h6>
                                            <p class="mb-0 small" style="color: var(--secondary);">Desktop, tablet, or mobile</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}