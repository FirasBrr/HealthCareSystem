{% extends 'base.html.twig' %}

{% block title %}MedicalApp - Patients Management{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        :root {
            --primary: #2c7be5;
            --primary-light: #e3ebf8;
            --primary-dark: #1a68d1;
            --secondary: #6e84a3;
            --success: #00d97e;
            --success-light: #e6f9f0;
            --warning: #f6c343;
            --warning-light: #fef8e6;
            --danger: #e63757;
            --danger-light: #fce9ec;
            --info: #39afd1;
            --info-light: #eaf5f9;
            --light: #f9fbfd;
            --dark: #12263f;
            --gray-100: #f8f9fa;
            --gray-200: #e9ecef;
            --gray-300: #dee2e6;
            --gray-400: #ced4da;
            --gray-500: #adb5bd;
            --gray-600: #6c757d;
            --gray-700: #495057;
            --gray-800: #343a40;
            --gray-900: #212529;
            --admin-primary: #4361ee;
            --admin-primary-light: #edf2ff;
            --admin-primary-dark: #3a56d4;
            --card-bg: #ffffff;
            --body-bg: #f5f7fb;
            --sidebar-bg: #ffffff;
            --gradient-primary: linear-gradient(135deg, #2c7be5 0%, #1a68d1 100%);
            --gradient-success: linear-gradient(135deg, #00d97e 0%, #00b86c 100%);
            --gradient-warning: linear-gradient(135deg, #f6c343 0%, #f4b223 100%);
            --gradient-danger: linear-gradient(135deg, #e63757 0%, #d92545 100%);
            --gradient-admin: linear-gradient(135deg, #4361ee 0%, #3a56d4 100%);
            --shadow-sm: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            --shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            --shadow-lg: 0 1rem 3rem rgba(0, 0, 0, 0.175);
            --border-radius: 0.75rem;
            --border-radius-sm: 0.5rem;
            --transition: all 0.3s ease;
        }

        body {
            background-color: var(--body-bg);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        /* Main Dashboard Layout */
        .dashboard-wrapper {
            display: flex;
            min-height: 100vh;
        }

        /* Sidebar Navigation */
        .sidebar {
            width: 280px;
            background: var(--sidebar-bg);
            border-right: 1px solid var(--gray-200);
            position: fixed;
            height: 100vh;
            left: 0;
            top: 0;
            z-index: 1000;
            display: flex;
            flex-direction: column;
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        .sidebar-header {
            padding: 1.5rem;
            border-bottom: 1px solid var(--gray-200);
            display: flex;
            align-items: center;
            gap: 1rem;
            background: var(--gradient-admin);
            color: white;
        }

        .admin-avatar {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.25rem;
            font-weight: 600;
            backdrop-filter: blur(10px);
        }

        .admin-info h6 {
            margin-bottom: 0.25rem;
            font-weight: 600;
            color: white;
        }

        .admin-info small {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.85rem;
        }

        .sidebar-nav {
            flex: 1;
            padding: 1.5rem;
            overflow-y: auto;
        }

        .nav-section {
            margin-bottom: 2rem;
        }

        .nav-section-title {
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            color: var(--gray-600);
            letter-spacing: 0.5px;
            margin-bottom: 1rem;
        }

        .nav-item {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            border-radius: var(--border-radius-sm);
            color: var(--gray-700);
            text-decoration: none;
            margin-bottom: 0.5rem;
            transition: var(--transition);
            font-weight: 500;
        }

        .nav-item:hover {
            background: var(--admin-primary-light);
            color: var(--admin-primary-dark);
            transform: translateX(5px);
        }

        .nav-item.active {
            background: var(--gradient-admin);
            color: white;
            box-shadow: 0 4px 12px rgba(67, 97, 238, 0.3);
        }

        .nav-icon {
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 1rem;
            background: var(--gray-100);
            color: var(--gray-600);
            transition: var(--transition);
        }

        .nav-item:hover .nav-icon {
            background: white;
        }

        .nav-item.active .nav-icon {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }

        .sidebar-footer {
            padding: 1.5rem;
            border-top: 1px solid var(--gray-200);
        }

        .logout-btn {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            border-radius: var(--border-radius-sm);
            color: var(--danger);
            text-decoration: none;
            font-weight: 500;
            transition: var(--transition);
        }

        .logout-btn:hover {
            background: var(--danger-light);
        }

        /* Main Content Area */
        .main-content {
            flex: 1;
            margin-left: 280px;
            padding: 2rem;
            max-width: calc(100% - 280px);
        }

        /* Top Header */
        .top-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--gray-200);
        }

        .header-left h1 {
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }

        .header-left p {
            color: var(--gray-600);
            margin: 0;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .date-badge {
            background: var(--gradient-admin);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-weight: 500;
            font-size: 0.9rem;
            box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
        }

        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 1.5rem;
            box-shadow: var(--shadow);
            border: 1px solid var(--gray-200);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100%;
        }

        .stat-card.total::before { background: var(--primary); }
        .stat-card.appointments::before { background: var(--success); }
        .stat-card.upcoming::before { background: var(--warning); }
        .stat-card.active::before { background: var(--info); }

        .stat-content {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
        }

        .stat-icon {
            width: 48px;
            height: 48px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 1rem;
        }

        .stat-card.total .stat-icon {
            background: var(--primary-light);
            color: var(--primary);
        }

        .stat-card.appointments .stat-icon {
            background: var(--success-light);
            color: var(--success);
        }

        .stat-card.upcoming .stat-icon {
            background: var(--warning-light);
            color: var(--warning);
        }

        .stat-card.active .stat-icon {
            background: var(--info-light);
            color: var(--info);
        }

        .stat-numbers h3 {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.25rem;
            color: var(--dark);
        }

        .stat-numbers p {
            color: var(--gray-600);
            font-size: 0.9rem;
            margin: 0;
        }

        /* Page Header */
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding: 1.5rem;
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow);
            border: 1px solid var(--gray-200);
        }

        .page-title h2 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--dark);
            margin: 0;
        }

        .page-title p {
            color: var(--gray-600);
            margin: 0.5rem 0 0 0;
            font-size: 0.95rem;
        }

        .page-actions {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        .stats-counter {
            background: var(--admin-primary-light);
            color: var(--admin-primary);
            padding: 0.5rem 1rem;
            border-radius: 50px;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-add {
            background: var(--gradient-admin);
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: var(--border-radius-sm);
            font-weight: 500;
            font-size: 0.9rem;
            transition: var(--transition);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-add:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(67, 97, 238, 0.3);
        }

        /* Patients Grid */
        .patients-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .patient-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 1.5rem;
            box-shadow: var(--shadow);
            border: 1px solid var(--gray-200);
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .patient-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-lg);
            border-color: var(--admin-primary-light);
        }

        .patient-card::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 4px;
            height: 100%;
            background: var(--gradient-success);
        }

        .patient-header {
            display: flex;
            align-items: flex-start;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .patient-avatar {
            width: 60px;
            height: 60px;
            border-radius: 12px;
            background: var(--gradient-success);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            font-weight: 600;
            flex-shrink: 0;
        }

        .patient-info {
            flex: 1;
        }

        .patient-name {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--dark);
            margin: 0 0 0.5rem 0;
        }

        .appointment-badge {
            background: var(--primary-light);
            color: var(--primary);
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            margin-bottom: 0.5rem;
        }

        .upcoming-badge {
            background: var(--warning-light);
            color: var(--warning);
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 500;
            display: inline-flex;
            align-items: center;
            gap: 0.25rem;
            margin-left: 0.5rem;
        }

        .contact-info {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            margin: 1rem 0;
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.85rem;
            color: var(--gray-600);
        }

        .contact-item i {
            width: 16px;
            color: var(--gray-500);
        }

        .address-text {
            font-size: 0.85rem;
            color: var(--gray-600);
            line-height: 1.4;
        }

        .patient-footer {
            display: flex;
            gap: 0.5rem;
            margin-top: 1.5rem;
            padding-top: 1rem;
            border-top: 1px solid var(--gray-200);
        }

        .btn-action {
            padding: 0.5rem 0.75rem;
            border-radius: var(--border-radius-sm);
            font-size: 0.85rem;
            font-weight: 500;
            transition: var(--transition);
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            flex: 1;
        }

        .btn-edit {
            background: var(--admin-primary-light);
            color: var(--admin-primary);
            border: 1px solid var(--admin-primary-light);
        }

        .btn-edit:hover {
            background: var(--admin-primary);
            color: white;
            text-decoration: none;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(67, 97, 238, 0.2);
        }

        .btn-delete {
            background: var(--danger-light);
            color: var(--danger);
            border: 1px solid var(--danger-light);
        }

        .btn-delete:hover {
            background: var(--danger);
            color: white;
            text-decoration: none;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(230, 55, 87, 0.2);
        }

        /* Empty State */
        .empty-state {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 3rem 1.5rem;
            text-align: center;
            box-shadow: var(--shadow);
            border: 1px solid var(--gray-200);
            grid-column: 1 / -1;
        }

        .empty-icon {
            font-size: 3rem;
            color: var(--gray-400);
            margin-bottom: 1rem;
        }

        .empty-state h5 {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 0.5rem;
        }

        .empty-state p {
            color: var(--gray-600);
            margin-bottom: 1.5rem;
        }

        /* Alert Messages */
        .alert {
            border-radius: var(--border-radius-sm);
            border: none;
            box-shadow: var(--shadow-sm);
            margin-bottom: 1.5rem;
        }

        /* Toast Notification */
        .alert-toast {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            min-width: 300px;
            border-radius: var(--border-radius-sm);
            box-shadow: var(--shadow-lg);
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .sidebar {
                width: 250px;
            }
            .main-content {
                margin-left: 250px;
                max-width: calc(100% - 250px);
            }
            .patients-grid {
                grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            }
        }

        @media (max-width: 992px) {
            .dashboard-wrapper {
                flex-direction: column;
            }
            .sidebar {
                width: 100%;
                height: auto;
                position: static;
                margin-bottom: 1rem;
            }
            .main-content {
                margin-left: 0;
                max-width: 100%;
                padding: 1rem;
            }
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .patients-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
        }

        @media (max-width: 768px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }
            .top-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            .header-right {
                width: 100%;
                justify-content: space-between;
            }
            .page-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }
            .page-actions {
                width: 100%;
                justify-content: space-between;
            }
            .patients-grid {
                grid-template-columns: 1fr;
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .patient-card {
            animation: fadeIn 0.3s ease forwards;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
{% endblock %}

{% block body %}
    <div class="dashboard-wrapper">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="admin-avatar">
                    {{ app.user.firstName|first|upper }}{{ app.user.lastName|first|upper }}
                </div>
                <div class="admin-info">
                    <h6>{{ app.user.firstName }} {{ app.user.lastName }}</h6>
                    <small>Administrator</small>
                </div>
            </div>

            <nav class="sidebar-nav">
                <div class="nav-section">
                    <div class="nav-section-title">Dashboard</div>
                    <a href="{{ path('app_dashboard') }}" class="nav-item">
                        <div class="nav-icon">
                            <i class="fas fa-home"></i>
                        </div>
                        Overview
                    </a>
                    <a href="{{ path('admin_appointments') }}" class="nav-item">
                        <div class="nav-icon">
                            <i class="fas fa-calendar-alt"></i>
                        </div>
                        Appointments
                    </a>
                </div>

                <div class="nav-section">
                    <div class="nav-section-title">Management</div>
                    <a href="{{ path('admin_doctors') }}" class="nav-item">
                        <div class="nav-icon">
                            <i class="fas fa-user-md"></i>
                        </div>
                        Doctors
                    </a>
                    <a href="{{ path('admin_patients') }}" class="nav-item active">
                        <div class="nav-icon">
                            <i class="fas fa-procedures"></i>
                        </div>
                        Patients
                    </a>
                </div>
            </nav>

            <div class="sidebar-footer">
                <a href="{{ path('app_logout') }}" class="logout-btn">
                    <i class="fas fa-sign-out-alt me-2"></i> Logout
                </a>
            </div>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Top Header -->
            <div class="top-header">
                <div class="header-left">
                    <h1>Patients Management</h1>
                    <p>View and manage all registered patients</p>
                </div>
                <div class="header-right">
                    <div class="date-badge">
                        <i class="fas fa-calendar-day me-2"></i>{{ "now"|date("l, F j, Y") }}
                    </div>
                </div>
            </div>


            <!-- Page Header -->
            <div class="page-header">
                <div class="page-title">
                    <h2>Patients Directory</h2>
                    <p>Manage patient information and records</p>
                </div>
                <div class="page-actions">
                    <div class="stats-counter">
                        <i class="fas fa-procedures me-2"></i>{{ patients|length }} Patients
                    </div>
                    <button type="button" class="btn-add" data-action="new">
                        <i class="fas fa-plus me-2"></i>Add New Patient
                    </button>
                </div>
            </div>

            <!-- Flash Messages -->
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <i class="fas fa-check-circle me-2"></i>{{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            {% for message in app.flashes('error') %}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="fas fa-exclamation-triangle me-2"></i>{{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            <!-- Patients Grid -->
            <div class="patients-grid" id="patients-container">
                {% for patient in patients %}
                    <div class="patient-card" id="patient-{{ patient.id }}">
                        <div class="patient-header">
                            <div class="patient-avatar">
                                {{ patient.client.firstName|first|upper }}{{ patient.client.lastName|first|upper }}
                            </div>
                            <div class="patient-info">
                                <h4 class="patient-name">{{ patient.client.firstName }} {{ patient.client.lastName }}</h4>
                                <div class="d-flex align-items-center">
                                    <span class="appointment-badge">
                                        <i class="fas fa-calendar-check"></i>
                                        {{ patient.appointments|length }} Appointments
                                    </span>
                                    {% set hasUpcoming = false %}
                                    {% set now = "now"|date('Y-m-d H:i:s') %}
                                    {% for appointment in patient.appointments %}
                                        {% if appointment.startDateTime|date('Y-m-d H:i:s') > now and appointment.status == 'Confirmed' %}
                                            {% set hasUpcoming = true %}
                                        {% endif %}
                                    {% endfor %}
                                    {% if hasUpcoming %}
                                        <span class="upcoming-badge">
                                            <i class="fas fa-clock"></i> Upcoming
                                        </span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        <div class="contact-info">
                            <div class="contact-item">
                                <i class="fas fa-envelope"></i>
                                <span>{{ patient.client.email }}</span>
                            </div>
                            <div class="contact-item">
                                <i class="fas fa-phone"></i>
                                <span>{{ patient.phone|default('Phone not provided') }}</span>
                            </div>
                            <div class="contact-item">
                                <i class="fas fa-map-marker-alt"></i>
                                <span class="address-text">
                                    {{ patient.address|default('Address not provided') }}
                                </span>
                            </div>
                        </div>

                        <div class="patient-footer">
                            <button type="button" class="btn-action btn-edit edit-patient"
                                    data-id="{{ patient.id }}">
                                <i class="fas fa-edit me-1"></i>Edit
                            </button>
                            <form method="post" action="{{ path('admin_patients_delete', {id: patient.id}) }}"
                                  class="delete-form d-inline"
                                  onsubmit="return confirm('Are you sure you want to delete {{ patient.client.firstName }} {{ patient.client.lastName }}?')">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patient.id) }}">
                                <button type="submit" class="btn-action btn-delete">
                                    <i class="fas fa-trash me-1"></i>Delete
                                </button>
                            </form>
                        </div>
                    </div>
                {% else %}
                    <!-- Empty State -->
                    <div class="empty-state">
                        <i class="fas fa-procedures empty-icon"></i>
                        <h5>No patients registered yet</h5>
                        <p>Start by adding your first patient to the platform.</p>
                        <button type="button" class="btn-add" data-action="new">
                            <i class="fas fa-plus me-2"></i>Add First Patient
                        </button>
                    </div>
                {% endfor %}
            </div>
        </main>
    </div>

    <!-- Patient Modal -->
    <div class="modal fade" id="patientModal" tabindex="-1" aria-labelledby="patientModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title fw-bold" id="patientModalLabel">Add New Patient</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body" id="modal-body-content">
                    <!-- Form will be loaded here via AJAX -->
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const modal = new bootstrap.Modal(document.getElementById('patientModal'));
            const modalBody = document.getElementById('modal-body-content');
            const modalTitle = document.getElementById('patientModalLabel');
            const patientsContainer = document.getElementById('patients-container');

            // Show toast notification
            function showToast(message, type = 'success') {
                const toast = document.createElement('div');
                toast.className = `alert alert-${type === 'success' ? 'success' : 'danger'} alert-dismissible fade show alert-toast`;
                toast.innerHTML = `
                    <i class="fas fa-${type === 'success' ? 'check-circle' : 'exclamation-triangle'} me-2"></i>
                    ${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                `;
                document.body.appendChild(toast);

                // Auto remove after 5 seconds
                setTimeout(() => {
                    if (toast.parentNode) {
                        toast.remove();
                    }
                }, 5000);
            }

            // Load form in modal
            async function loadForm(url, title) {
                try {
                    modalTitle.textContent = title;
                    modalBody.innerHTML = '<div class="text-center py-5"><div class="spinner-border text-primary" role="status"></div><p class="mt-2 text-muted">Loading...</p></div>';

                    const response = await fetch(url, {
                        headers: {
                            'X-Requested-With': 'XMLHttpRequest'
                        }
                    });

                    if (!response.ok) {
                        throw new Error('Network response was not ok');
                    }

                    const data = await response.json();

                    if (data.html) {
                        modalBody.innerHTML = data.html;
                        modal.show();

                        // Initialize form submission handler
                        const form = modalBody.querySelector('form');
                        if (form) {
                            form.addEventListener('submit', handleFormSubmit);
                        }
                    } else {
                        throw new Error('No HTML content received');
                    }
                } catch (error) {
                    console.error('Error loading form:', error);
                    modalBody.innerHTML = `
                        <div class="alert alert-danger">
                            <h6><i class="fas fa-exclamation-triangle me-2"></i>Error Loading Form</h6>
                            <p class="mb-0">There was an error loading the form. Please try again.</p>
                        </div>
                    `;
                    modal.show();
                }
            }

            // Handle form submission
            async function handleFormSubmit(e) {
                e.preventDefault();
                const form = e.target;
                const submitBtn = form.querySelector('.btn-submit');
                const formData = new FormData(form);

                // Show loading state
                if (submitBtn) {
                    submitBtn.classList.add('btn-loading');
                    submitBtn.disabled = true;
                    const originalText = submitBtn.innerHTML;
                    submitBtn.innerHTML = '<div class="spinner-border spinner-border-sm me-2"></div> Processing...';
                }

                try {
                    const response = await fetch(form.action, {
                        method: 'POST',
                        body: formData,
                        headers: {
                            'X-Requested-With': 'XMLHttpRequest'
                        }
                    });

                    const data = await response.json();

                    if (data.success) {
                        showToast(data.message);
                        modal.hide();

                        // Reload the page to show updated data
                        setTimeout(() => {
                            window.location.reload();
                        }, 1000);
                    } else {
                        // Update form with validation errors
                        modalBody.innerHTML = data.html;

                        // Re-initialize form submission handler
                        const newForm = modalBody.querySelector('form');
                        if (newForm) {
                            newForm.addEventListener('submit', handleFormSubmit);
                        }

                        showToast('Please check the form for errors', 'error');
                    }
                } catch (error) {
                    console.error('Error submitting form:', error);
                    showToast('Error submitting form. Please try again.', 'error');
                } finally {
                    if (submitBtn) {
                        submitBtn.classList.remove('btn-loading');
                        submitBtn.disabled = false;
                        submitBtn.innerHTML = 'Add Patient';
                    }
                }
            }

            // Add new patient
            document.querySelectorAll('[data-action="new"]').forEach(btn => {
                btn.addEventListener('click', () => {
                    loadForm('{{ path('admin_patients_new') }}?ajax=1', 'Add New Patient');
                });
            });

            // Edit patient
            document.querySelectorAll('.edit-patient').forEach(btn => {
                btn.addEventListener('click', () => {
                    const patientId = btn.getAttribute('data-id');
                    loadForm(`/admin/patients/${patientId}/edit?ajax=1`, 'Edit Patient');
                });
            });

            // Add animation to patient cards
            const patientCards = document.querySelectorAll('.patient-card');
            patientCards.forEach((card, index) => {
                card.style.animationDelay = `${index * 0.05}s`;
            });

            // Add animation to stats cards
            const statCards = document.querySelectorAll('.stat-card');
            statCards.forEach((card, index) => {
                card.style.animationDelay = `${index * 0.1}s`;
            });
        });
    </script>
{% endblock %}